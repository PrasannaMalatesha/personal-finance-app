# Leave VITE_API_BASE_URL empty in dev — Vite dev server proxies /api and /healthz to localhost:3001.
# In prod (Vercel), set to the deployed backend origin (no trailing /api/v1).
VITE_API_BASE_URL=

VITE_SENTRY_DSN=

VITE_FLAG_PLAID=false
VITE_FLAG_RECURRING=false
VITE_FLAG_MULTI_CURRENCY=false
